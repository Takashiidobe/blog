<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="406" onload="init(evt)" viewBox="0 0 1200 406" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="406" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="389.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="389.00"> </text><svg id="frames" x="10" width="1180" total_samples="332894165"><g><title>core::slice::sort::shared::smallsort::small_sort_general::&lt;obrc::StationResult, &lt;[obrc::StationResult]&gt;::sort_unstable_by&lt;&lt;obrc::solvers::fnv_hashmap::FnvHashMapSolver as obrc::Solver&gt;::finish::{closure#1}&gt;::{closure#0}&gt; (4,495,940 samples, 1.35%)</title><rect x="1.3823%" y="85" width="1.3506%" height="15" fill="rgb(227,0,7)" fg:x="4601479" fg:w="4495940"/><text x="1.6323%" y="95.50"></text></g><g><title>&lt;obrc::solvers::basic_hashmap::BasicHashMapSolver as obrc::Solver&gt;::finish (13,277,014 samples, 3.99%)</title><rect x="0.0038%" y="165" width="3.9884%" height="15" fill="rgb(217,0,24)" fg:x="12642" fg:w="13277014"/><text x="0.2538%" y="175.50">&lt;obr..</text></g><g><title>core::slice::sort::unstable::quicksort::quicksort::&lt;obrc::StationResult, &lt;[obrc::StationResult]&gt;::sort_unstable_by&lt;&lt;obrc::solvers::basic_hashmap::BasicHashMapSolver as obrc::Solver&gt;::finish::{closure#1}&gt;::{closure#0}&gt; (13,277,014 samples, 3.99%)</title><rect x="0.0038%" y="149" width="3.9884%" height="15" fill="rgb(221,193,54)" fg:x="12642" fg:w="13277014"/><text x="0.2538%" y="159.50">core..</text></g><g><title>core::slice::sort::unstable::quicksort::quicksort::&lt;obrc::StationResult, &lt;[obrc::StationResult]&gt;::sort_unstable_by&lt;&lt;obrc::solvers::basic_hashmap::BasicHashMapSolver as obrc::Solver&gt;::finish::{closure#1}&gt;::{closure#0}&gt; (8,688,177 samples, 2.61%)</title><rect x="1.3823%" y="133" width="2.6099%" height="15" fill="rgb(248,212,6)" fg:x="4601479" fg:w="8688177"/><text x="1.6323%" y="143.50">co..</text></g><g><title>core::slice::sort::unstable::quicksort::quicksort::&lt;obrc::StationResult, &lt;[obrc::StationResult]&gt;::sort_unstable_by&lt;&lt;obrc::solvers::basic_hashmap::BasicHashMapSolver as obrc::Solver&gt;::finish::{closure#1}&gt;::{closure#0}&gt; (8,688,177 samples, 2.61%)</title><rect x="1.3823%" y="117" width="2.6099%" height="15" fill="rgb(208,68,35)" fg:x="4601479" fg:w="8688177"/><text x="1.6323%" y="127.50">co..</text></g><g><title>core::slice::sort::unstable::quicksort::quicksort::&lt;obrc::StationResult, &lt;[obrc::StationResult]&gt;::sort_unstable_by&lt;&lt;obrc::solvers::basic_hashmap::BasicHashMapSolver as obrc::Solver&gt;::finish::{closure#1}&gt;::{closure#0}&gt; (8,688,177 samples, 2.61%)</title><rect x="1.3823%" y="101" width="2.6099%" height="15" fill="rgb(232,128,0)" fg:x="4601479" fg:w="8688177"/><text x="1.6323%" y="111.50">co..</text></g><g><title>core::slice::sort::unstable::quicksort::quicksort::&lt;obrc::StationResult, &lt;[obrc::StationResult]&gt;::sort_unstable_by&lt;&lt;obrc::solvers::basic_hashmap::BasicHashMapSolver as obrc::Solver&gt;::finish::{closure#1}&gt;::{closure#0}&gt; (4,192,237 samples, 1.26%)</title><rect x="2.7328%" y="85" width="1.2593%" height="15" fill="rgb(207,160,47)" fg:x="9097419" fg:w="4192237"/><text x="2.9828%" y="95.50"></text></g><g><title>core::slice::sort::unstable::quicksort::quicksort::&lt;obrc::StationResult, &lt;[obrc::StationResult]&gt;::sort_unstable_by&lt;&lt;obrc::solvers::basic_hashmap::BasicHashMapSolver as obrc::Solver&gt;::finish::{closure#1}&gt;::{closure#0}&gt; (4,192,237 samples, 1.26%)</title><rect x="2.7328%" y="69" width="1.2593%" height="15" fill="rgb(228,23,34)" fg:x="9097419" fg:w="4192237"/><text x="2.9828%" y="79.50"></text></g><g><title>[libc.so.6] (4,192,237 samples, 1.26%)</title><rect x="2.7328%" y="53" width="1.2593%" height="15" fill="rgb(218,30,26)" fg:x="9097419" fg:w="4192237"/><text x="2.9828%" y="63.50"></text></g><g><title>[libc.so.6] (4,500,667 samples, 1.35%)</title><rect x="3.9922%" y="165" width="1.3520%" height="15" fill="rgb(220,122,19)" fg:x="13289656" fg:w="4500667"/><text x="4.2422%" y="175.50"></text></g><g><title>[libc.so.6] (4,500,667 samples, 1.35%)</title><rect x="3.9922%" y="149" width="1.3520%" height="15" fill="rgb(250,228,42)" fg:x="13289656" fg:w="4500667"/><text x="4.2422%" y="159.50"></text></g><g><title>[libc.so.6] (4,500,667 samples, 1.35%)</title><rect x="3.9922%" y="133" width="1.3520%" height="15" fill="rgb(240,193,28)" fg:x="13289656" fg:w="4500667"/><text x="4.2422%" y="143.50"></text></g><g><title>&lt;std::hash::random::RandomState as core::hash::BuildHasher&gt;::hash_one::&lt;&amp;alloc::string::String&gt; (6,448,972 samples, 1.94%)</title><rect x="43.5424%" y="101" width="1.9372%" height="15" fill="rgb(216,20,37)" fg:x="144950036" fg:w="6448972"/><text x="43.7924%" y="111.50">&lt;..</text></g><g><title>&lt;core::hash::sip::Hasher&lt;core::hash::sip::Sip13Rounds&gt; as core::hash::Hasher&gt;::write (38,799 samples, 0.01%)</title><rect x="45.4680%" y="85" width="0.0117%" height="15" fill="rgb(206,188,39)" fg:x="151360209" fg:w="38799"/><text x="45.7180%" y="95.50"></text></g><g><title>&lt;hashbrown::raw::RawTable&lt;(alloc::string::String, obrc::solvers::basic_hashmap::Stats)&gt;&gt;::reserve_rehash::&lt;hashbrown::map::make_hasher&lt;alloc::string::String, obrc::solvers::basic_hashmap::Stats, std::hash::random::RandomState&gt;::{closure#0}&gt; (10,499,934 samples, 3.15%)</title><rect x="43.5424%" y="117" width="3.1541%" height="15" fill="rgb(217,207,13)" fg:x="144950008" fg:w="10499934"/><text x="43.7924%" y="127.50">&lt;ha..</text></g><g><title>[unknown] (4,050,934 samples, 1.22%)</title><rect x="45.4796%" y="101" width="1.2169%" height="15" fill="rgb(231,73,38)" fg:x="151399008" fg:w="4050934"/><text x="45.7296%" y="111.50"></text></g><g><title>&lt;hashbrown::map::HashMap&lt;alloc::string::String, obrc::solvers::basic_hashmap::Stats, std::hash::random::RandomState&gt;&gt;::insert (15,148,705 samples, 4.55%)</title><rect x="43.5424%" y="133" width="4.5506%" height="15" fill="rgb(225,20,46)" fg:x="144950008" fg:w="15148705"/><text x="43.7924%" y="143.50">&lt;hash..</text></g><g><title>&lt;std::hash::random::RandomState as core::hash::BuildHasher&gt;::hash_one::&lt;&amp;alloc::string::String&gt; (4,648,771 samples, 1.40%)</title><rect x="46.6965%" y="117" width="1.3965%" height="15" fill="rgb(210,31,41)" fg:x="155449942" fg:w="4648771"/><text x="46.9465%" y="127.50"></text></g><g><title>&lt;std::hash::random::RandomState as core::hash::BuildHasher&gt;::hash_one::&lt;&amp;str&gt; (73,927,815 samples, 22.21%)</title><rect x="48.0930%" y="133" width="22.2076%" height="15" fill="rgb(221,200,47)" fg:x="160098713" fg:w="73927815"/><text x="48.3430%" y="143.50">&lt;std::hash::random::RandomState as ..</text></g><g><title>&lt;core::hash::sip::Hasher&lt;core::hash::sip::Sip13Rounds&gt; as core::hash::Hasher&gt;::write (46,159,639 samples, 13.87%)</title><rect x="56.4344%" y="117" width="13.8662%" height="15" fill="rgb(226,26,5)" fg:x="187866889" fg:w="46159639"/><text x="56.6844%" y="127.50">&lt;core::hash::sip::Has..</text></g><g><title>&lt;obrc::solvers::basic_hashmap::BasicHashMapSolver as obrc::Solver&gt;::process (155,697,182 samples, 46.77%)</title><rect x="30.5597%" y="149" width="46.7708%" height="15" fill="rgb(249,33,26)" fg:x="101731551" fg:w="155697182"/><text x="30.8097%" y="159.50">&lt;obrc::solvers::basic_hashmap::BasicHashMapSolver as obrc::Solver&gt;::process</text></g><g><title>[libc.so.6] (23,402,205 samples, 7.03%)</title><rect x="70.3006%" y="133" width="7.0299%" height="15" fill="rgb(235,183,28)" fg:x="234026528" fg:w="23402205"/><text x="70.5506%" y="143.50">[libc.so...</text></g><g><title>core::str::converts::from_utf8 (10,201,449 samples, 3.06%)</title><rect x="77.3305%" y="149" width="3.0645%" height="15" fill="rgb(221,5,38)" fg:x="257428733" fg:w="10201449"/><text x="77.5805%" y="159.50">cor..</text></g><g><title>obrc::input::process_measurements_bytes::&lt;obrc::solvers::basic_hashmap::BasicHashMapSolver&gt; (261,975,828 samples, 78.70%)</title><rect x="5.3441%" y="165" width="78.6964%" height="15" fill="rgb(247,18,42)" fg:x="17790323" fg:w="261975828"/><text x="5.5941%" y="175.50">obrc::input::process_measurements_bytes::&lt;obrc::solvers::basic_hashmap::BasicHashMapSolver&gt;</text></g><g><title>memchr::arch::x86_64::memchr::memchr_raw::find_avx2 (12,135,969 samples, 3.65%)</title><rect x="80.3950%" y="149" width="3.6456%" height="15" fill="rgb(241,131,45)" fg:x="267630182" fg:w="12135969"/><text x="80.6450%" y="159.50">memc..</text></g><g><title>&lt;memchr::arch::x86_64::avx2::memchr::One&gt;::find_raw (4,615,864 samples, 1.39%)</title><rect x="82.6540%" y="133" width="1.3866%" height="15" fill="rgb(249,31,29)" fg:x="275150287" fg:w="4615864"/><text x="82.9040%" y="143.50"></text></g><g><title>&lt;core::fmt::Formatter&gt;::write_formatted_parts (4,491,633 samples, 1.35%)</title><rect x="92.0561%" y="101" width="1.3493%" height="15" fill="rgb(225,111,53)" fg:x="306449477" fg:w="4491633"/><text x="92.3061%" y="111.50"></text></g><g><title>&lt;alloc::string::String as core::fmt::Write&gt;::write_str (4,491,633 samples, 1.35%)</title><rect x="92.0561%" y="85" width="1.3493%" height="15" fill="rgb(238,160,17)" fg:x="306449477" fg:w="4491633"/><text x="92.3061%" y="95.50"></text></g><g><title>&lt;alloc::raw_vec::RawVecInner&lt;_&gt;&gt;::reserve::do_reserve_and_handle::&lt;alloc::alloc::Global&gt; (4,491,633 samples, 1.35%)</title><rect x="92.0561%" y="69" width="1.3493%" height="15" fill="rgb(214,148,48)" fg:x="306449477" fg:w="4491633"/><text x="92.3061%" y="79.50"></text></g><g><title>&lt;alloc::raw_vec::RawVecInner&gt;::finish_grow (4,491,633 samples, 1.35%)</title><rect x="92.0561%" y="53" width="1.3493%" height="15" fill="rgb(232,36,49)" fg:x="306449477" fg:w="4491633"/><text x="92.3061%" y="63.50"></text></g><g><title>__rustc::__rust_alloc (4,491,633 samples, 1.35%)</title><rect x="92.0561%" y="37" width="1.3493%" height="15" fill="rgb(209,103,24)" fg:x="306449477" fg:w="4491633"/><text x="92.3061%" y="47.50"></text></g><g><title>core::num::imp::flt2dec::strategy::dragon::format_exact (4,266,827 samples, 1.28%)</title><rect x="93.4054%" y="101" width="1.2817%" height="15" fill="rgb(229,88,8)" fg:x="310941110" fg:w="4266827"/><text x="93.6554%" y="111.50"></text></g><g><title>&lt;core::num::imp::bignum::Big32x40&gt;::mul_pow2 (4,266,827 samples, 1.28%)</title><rect x="93.4054%" y="85" width="1.2817%" height="15" fill="rgb(213,181,19)" fg:x="310941110" fg:w="4266827"/><text x="93.6554%" y="95.50"></text></g><g><title>alloc::fmt::format::format_inner (44,364,970 samples, 13.33%)</title><rect x="84.0406%" y="149" width="13.3270%" height="15" fill="rgb(254,191,54)" fg:x="279766151" fg:w="44364970"/><text x="84.2906%" y="159.50">alloc::fmt::format::..</text></g><g><title>core::fmt::write (39,976,411 samples, 12.01%)</title><rect x="85.3589%" y="133" width="12.0087%" height="15" fill="rgb(241,83,37)" fg:x="284154710" fg:w="39976411"/><text x="85.6089%" y="143.50">core::fmt::write</text></g><g><title>core::fmt::float::float_to_decimal_common_exact::&lt;f64&gt; (35,560,439 samples, 10.68%)</title><rect x="86.6854%" y="117" width="10.6822%" height="15" fill="rgb(233,36,39)" fg:x="288570682" fg:w="35560439"/><text x="86.9354%" y="127.50">core::fmt::float..</text></g><g><title>core::num::imp::flt2dec::strategy::grisu::format_exact_opt (8,923,184 samples, 2.68%)</title><rect x="94.6871%" y="101" width="2.6805%" height="15" fill="rgb(226,3,54)" fg:x="315207937" fg:w="8923184"/><text x="94.9371%" y="111.50">co..</text></g><g><title>&lt;core::fmt::Formatter&gt;::pad (4,352,093 samples, 1.31%)</title><rect x="97.3676%" y="133" width="1.3074%" height="15" fill="rgb(245,192,40)" fg:x="324131121" fg:w="4352093"/><text x="97.6176%" y="143.50"></text></g><g><title>all (332,894,165 samples, 100%)</title><rect x="0.0000%" y="357" width="100.0000%" height="15" fill="rgb(238,167,29)" fg:x="0" fg:w="332894165"/><text x="0.2500%" y="367.50"></text></g><g><title>hashmap-mmap (332,894,165 samples, 100.00%)</title><rect x="0.0000%" y="341" width="100.0000%" height="15" fill="rgb(232,182,51)" fg:x="0" fg:w="332894165"/><text x="0.2500%" y="351.50">hashmap-mmap</text></g><g><title>_start (332,881,523 samples, 100.00%)</title><rect x="0.0038%" y="325" width="99.9962%" height="15" fill="rgb(231,60,39)" fg:x="12642" fg:w="332881523"/><text x="0.2538%" y="335.50">_start</text></g><g><title>__libc_start_main (332,881,523 samples, 100.00%)</title><rect x="0.0038%" y="309" width="99.9962%" height="15" fill="rgb(208,69,12)" fg:x="12642" fg:w="332881523"/><text x="0.2538%" y="319.50">__libc_start_main</text></g><g><title>[libc.so.6] (332,881,523 samples, 100.00%)</title><rect x="0.0038%" y="293" width="99.9962%" height="15" fill="rgb(235,93,37)" fg:x="12642" fg:w="332881523"/><text x="0.2538%" y="303.50">[libc.so.6]</text></g><g><title>main (332,881,523 samples, 100.00%)</title><rect x="0.0038%" y="277" width="99.9962%" height="15" fill="rgb(213,116,39)" fg:x="12642" fg:w="332881523"/><text x="0.2538%" y="287.50">main</text></g><g><title>std::rt::lang_start_internal (332,881,523 samples, 100.00%)</title><rect x="0.0038%" y="261" width="99.9962%" height="15" fill="rgb(222,207,29)" fg:x="12642" fg:w="332881523"/><text x="0.2538%" y="271.50">std::rt::lang_start_internal</text></g><g><title>std::rt::lang_start::&lt;core::result::Result&lt;(), std::io::error::Error&gt;&gt;::{closure#0} (332,881,523 samples, 100.00%)</title><rect x="0.0038%" y="245" width="99.9962%" height="15" fill="rgb(206,96,30)" fg:x="12642" fg:w="332881523"/><text x="0.2538%" y="255.50">std::rt::lang_start::&lt;core::result::Result&lt;(), std::io::error::Error&gt;&gt;::{closure#0}</text></g><g><title>std::sys::backtrace::__rust_begin_short_backtrace::&lt;fn()  (332,881,523 samples, 100.00%)</title><rect x="0.0038%" y="229" width="99.9962%" height="15" fill="rgb(218,138,4)" fg:x="12642" fg:w="332881523"/><text x="0.2538%" y="239.50">std::sys::backtrace::__rust_begin_short_backtrace::&lt;fn() </text></g><g><title> core::result::Result&lt;(), std::io::error::Error&gt;, core::result::Result&lt;(), std::io::error::Error&gt;&gt; (332,881,523 samples, 100.00%)</title><rect x="0.0038%" y="213" width="99.9962%" height="15" fill="rgb(250,191,14)" fg:x="12642" fg:w="332881523"/><text x="0.2538%" y="223.50"> core::result::Result&lt;(), std::io::error::Error&gt;, core::result::Result&lt;(), std::io::error::Error&gt;&gt;</text></g><g><title>hashmap_mmap::main (332,881,523 samples, 100.00%)</title><rect x="0.0038%" y="197" width="99.9962%" height="15" fill="rgb(239,60,40)" fg:x="12642" fg:w="332881523"/><text x="0.2538%" y="207.50">hashmap_mmap::main</text></g><g><title>obrc::runner::run_solver_mmap::&lt;obrc::solvers::basic_hashmap::BasicHashMapSolver&gt; (332,881,523 samples, 100.00%)</title><rect x="0.0038%" y="181" width="99.9962%" height="15" fill="rgb(206,27,48)" fg:x="12642" fg:w="332881523"/><text x="0.2538%" y="191.50">obrc::runner::run_solver_mmap::&lt;obrc::solvers::basic_hashmap::BasicHashMapSolver&gt;</text></g><g><title>obrc::output::write_results_to_path (53,128,014 samples, 15.96%)</title><rect x="84.0406%" y="165" width="15.9594%" height="15" fill="rgb(225,35,8)" fg:x="279766151" fg:w="53128014"/><text x="84.2906%" y="175.50">obrc::output::write_resul..</text></g><g><title>core::fmt::write (8,763,044 samples, 2.63%)</title><rect x="97.3676%" y="149" width="2.6324%" height="15" fill="rgb(250,213,24)" fg:x="324131121" fg:w="8763044"/><text x="97.6176%" y="159.50">co..</text></g><g><title>&lt;std::io::default_write_fmt::Adapter&lt;std::io::buffered::bufwriter::BufWriter&lt;std::fs::File&gt;&gt; as core::fmt::Write&gt;::write_str (4,410,951 samples, 1.33%)</title><rect x="98.6750%" y="133" width="1.3250%" height="15" fill="rgb(247,123,22)" fg:x="328483214" fg:w="4410951"/><text x="98.9250%" y="143.50"></text></g><g><title>[libc.so.6] (4,410,951 samples, 1.33%)</title><rect x="98.6750%" y="117" width="1.3250%" height="15" fill="rgb(231,138,38)" fg:x="328483214" fg:w="4410951"/><text x="98.9250%" y="127.50"></text></g></svg></svg>